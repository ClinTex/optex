package views;

import haxe.ui.containers.VBox;

@:build(haxe.ui.ComponentBuilder.build("assets/views/settings.xml"))
class SettingsView extends VBox {
    public function new() {
        super();
    }
}
